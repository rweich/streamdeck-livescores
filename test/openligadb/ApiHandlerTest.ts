import 'mocha';
import ApiHandler from '../../src/openligadb/ApiHandler';

describe('sendEvent', () => {
  it('foo', () => {
    // const p = new Streamdeck().plugin();
    const a = new ApiHandler();
  });
});
